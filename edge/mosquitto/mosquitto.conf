# Listen on all interfaces so the deepstream container can reach us over the
# compose network (host "mosquitto", port 1883).
listener 1883 0.0.0.0
# No credentials needed for this tutorial. Do NOT do this in production.
allow_anonymous true
